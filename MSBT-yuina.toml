# 初期表示モード: "fit"=画面に合わせて表示, "original"=原寸大(100%)
initial_display_mode = "fit"

# デバッグログを有効にするかどうか
enable_debug_log = false

# マウスホイールの拡大率（1回の回転あたりの倍率）
wheel_zoom_factor = 0.005

# 拡大表示の補間: true=滑らか(バイリニア), false=ピクセル等倍(ニアレスト)
smooth_zoom = true

# 起動時に新バージョンを確認するかどうか
check_updates = true
